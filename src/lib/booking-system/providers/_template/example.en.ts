/**
 * TEMPLATE — English texts for the provider's Polish `t("…")` keys. Needed
 * only when the provider writes its texts in Polish; English-only providers
 * can skip this file.
 */
export const en: Record<string, string> = {};
